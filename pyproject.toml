[project]
name = "mtf-scoring-engine"
version = "0.1.0"
description = "Multi-timeframe technical analysis & scoring engine for Binance USDT-M Futures"
requires-python = ">=3.10"

[tool.pytest.ini_options]
testpaths = ["tests"]
addopts = "-q"
filterwarnings = [
    "ignore::DeprecationWarning",
]
